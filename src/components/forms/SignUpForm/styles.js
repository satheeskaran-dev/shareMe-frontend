import { styled } from "@mui/material";
import { FlexContainer } from "../../core/FlexItems";

export const Container = styled(FlexContainer)({
  flexDirection: "column",
  gap: "1.5rem",
  height: "37rem",
  padding: "2.5rem 2rem",
});
